# AI AGENTS READ THIS. 

Do not push. 

Git creds: Name: Anthony; git email: 33004321+anthony1x6000@users.noreply.github.com. 
Commit frequently and often. Commit with descriptive messages, and even more descriptive paragraphs. In the paragraphs, list exact what you've done, what lines were affected, etc.

As the AI agent, you must also review the commit history to determine what has been done before refactoring. Before making a change, if unsure, check the git history. 

## Refactoring project
The `stylustheme.css` is incredibly bloated and there is a lot of techical debt. You must refactor the stylesheet block by block, organizing everything into readable secitons. Ensure standard CSS3 is used, nothing fancy; therefore, review what CSS3 methods, functions, etc. were used in @stylustheme.css and use those and only those. 

Ensure the css is readable in the same way. 

## The goal of refactoring 
For the end user, the user which will never look at the code, nothing will change. All functionality must remain the same. The header must remain the same as well:
```
/* Hey! if you see this, check out the Github repository for this!
    https://github.com/anthony1x6000/ROBLOX2016stylus
    You can find the style under releases. Make sure to check out the beta "stylustheme.css."
    Make sure to report any bugs to the issue tracker on Github.  

    Credit to Ologist for initially making this great Userstyle theme!
    Code contributors:
    - Ologist (Original creator)
    - antho/anthony1x6000
    - Aubrey/aubymori
    - ssd21345/randomguyingithub
    - ProTaxEvader/ProTaxEvader
    - Parker/parker02311
    - Viper/Cosmella-v
    */ 
```

Ensure to keep `@-moz-document domain("web.roblox.com"), domain("www.roblox.com")` as well. 

The goal CSS code must be very readable and well organized. Group duplicate properties, etc. 

## Test cases 
Develop your own rigerous test cases and use the ones already provided; using python and bash; you can install and import external python libraries to help, to ensure the script retains functionality with `original-stylesheet.css`. 

You may break system packages with pip installs. 

## Comments
Within `stylustheme.css`, ensure to comment headers for each section, explaining what is happening. 
Header example: `/* CATALOG IMPLEMENTATION */` or `/* MISC BUG FIX */`. 

## Extra changes
Ensure `.css-1ugh0vn-textIconRowText[data-sdui-text="true"]` has fntweight 100