# AI 代理请阅读。

切勿执行 push 操作。

Git 凭据：Name: anthony1x6000; git email: 33004321+anthony1x6000@users.noreply.github.com. 
频繁且定期提交。提交时请附带描述性的消息，以及更详尽的段落说明。在段落中，请列出你所做的具体工作、受影响的行等。

作为 AI 代理，你必须在重构前审阅提交历史，以确定之前已完成的工作。在进行更改之前，如果不确定，请检查 git 历史。

## 重构项目
`stylustheme.css` 非常臃肿，且存在大量技术债务。你必须逐块重构样式表，将所有内容组织成易读的章节。确保使用标准的 CSS3，不要使用任何花哨的技巧；因此，请审阅 `@stylustheme.css` 中使用了哪些 CSS3 方法、函数等，并仅使用这些内容。

确保 CSS 以同样的方式保持易读性。

## 重构期间
在重构过程中，每 200 行压缩一次上下文，但要确保 CSS 属性和行保持完整。

## 重构目标
对于最终用户（即永远不会看代码的用户）来说，一切都不会改变。所有功能必须保持不变。`header` 也必须保持不变：

```
/* Hey! if you see this, check out the Github repository for this!
    https://github.com/anthony1x6000/ROBLOX2016stylus
    You can find the style under releases. Make sure to check out the beta "stylustheme.css."
    Make sure to report any bugs to the issue tracker on Github.  

    Credit to Ologist for initially making this great Userstyle theme!
    Code contributors:
    - Ologist (Original creator)
    - antho/anthony1x6000
    - Aubrey/aubymori
    - ssd21345/randomguyingithub
    - ProTaxEvader/ProTaxEvader
    - Parker/parker02311
    - Viper/Cosmella-v
    */ 
```

同时请务必保留 `@-moz-document domain("web.roblox.com"), domain("www.roblox.com")`。

最终的 CSS 代码必须非常易读且组织良好。合并重复的属性等。

## 测试用例
开发你自己的严谨测试用例并使用已提供的用例；使用 python 和 bash；你可以安装和导入外部 python 库来提供帮助，以确保脚本与 `original-stylesheet.css` 保持功能一致。

你可以通过 pip 安装破坏系统包。

## 注释
在 `stylustheme.css` 中，确保为每个章节编写注释头，解释正在发生的情况。
注释头示例：`/* CATALOG IMPLEMENTATION */` 或 `/* MISC BUG FIX */`。

## 额外更改
确保 `.css-1ugh0vn-textIconRowText[data-sdui-text="true"]` 的 `font-weight` 为 100。

---

**还需要我帮你把这段内容保存到特定的文件中，或者对重构逻辑做进一步的补充吗？**